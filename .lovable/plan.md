Revert the Review Queue duplicate-action button label from "Approve & Supersede" back to "Replace". The underlying behavior (approve new row, trigger DB supersede of older active siblings) stays unchanged; only the visible button text and tooltip are updated.