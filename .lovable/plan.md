

# Update Subtitle Text

## Change
In `src/components/projects/ProjectHeader.tsx`, update the subtitle from "Medical Practice Portal & Analytics" to "Clinic Portal and Analytics".

### File: `src/components/projects/ProjectHeader.tsx` (line 36)
Change the `<p>` text content from `Medical Practice Portal & Analytics` to `Clinic Portal and Analytics`.

